apiVersion: batch/v1
kind: Job
metadata:
  name: gwpulsar-sender-job
  labels:
    app: gwpulsar-sender
spec:
  template:
    metadata:
      labels:
        app: gwpulsar-sender
    spec:
      containers:
      - name: pulsar-sender
        image: fracasula/gwpulsar-sender:latest
        imagePullPolicy: Always
        env:
        - name: SERVER_ENDPOINT
          value: "http://gwpulsar-svc.fcasula.svc.cluster.local:8080"
        - name: NUM_SOURCES
          value: "100"
        - name: USERS_PER_SOURCE
          value: "5"
        - name: MESSAGES_PER_USER
          value: "300"
        - name: MODE
          value: "ORDER"
      imagePullSecrets:
      - name: regcred
      restartPolicy: Never
  backoffLimit: 4
